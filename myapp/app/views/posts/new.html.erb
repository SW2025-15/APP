<h1>新規投稿</h1>

<%= form_with(model: @post, local: true) do |f| %>
  <%# エラーメッセージの表示 %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= @post.errors.count %>件のエラーがあります。</h2>
      <ul>
        <% @post.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title, "タイトル" %>
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :body, "内容" %>
    <%= f.text_area :body %>
  </div>
  
  <%# 複数の画像アップロードフィールド %>
  <div>
    <%= f.label :images, "画像 (複数選択可)" %>
    <%= f.file_field :images, multiple: true %>
  </div>
  
  <%# status カラムは今回は省略します（必要であれば追加） %>

  <div>
    <%= f.submit "投稿を完了する" %>
  </div>
<% end %>

<%= link_to '一覧へ戻る', posts_path %>